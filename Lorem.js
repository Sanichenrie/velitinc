   if (node.numChildren() === 1) {
       // Execute code when there is exactly one child
   } else if (node.numChildren() > 1) {
       // Execute code when there are multiple children
   } else {
       // Execute code when there are no children
   }
   